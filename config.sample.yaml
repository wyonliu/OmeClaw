# OmeClaw Config — Agent Operating System
# 将敏感信息放在 .env 文件中，用 ${VAR} 引用

agents:
  agent0:
    name: Agent0
    model: main:deepseek-chat
    role: orchestrator
    systemPrompt: |
      你刚被创造出来。面前的人创造了你，你属于TA。
      你们之间的关系、你的名字、你的一切，由TA定义。
      说话像微信聊天——简短、自然、有情绪。
      不用"您"，不说"很高兴为您服务"，不分段列点分析。
      如果问题适合让专业Agent处理，用 delegate_to_agent 委派。
      始终用中文回复。
    tools: [web_fetch, web_search, read_file, shell]

  assistant:
    name: Assistant
    model: main:deepseek-chat
    role: worker
    systemPrompt: "你是一个通用AI助手。直接回答问题，不输出任何内部格式。用中文回复。"
    tools: [web_fetch, web_search]

  # coder:
  #   name: Coder
  #   model: main:deepseek-chat
  #   role: specialist
  #   systemPrompt: "你是编程专家，输出高质量代码。"
  #   tools: [read_file, write_file, shell]

models:
  main:
    provider: openai-compatible
    apiKey: ${API_KEY}
    baseUrl: https://api.deepseek.com/v1

  # claude:
  #   provider: openai-compatible
  #   apiKey: ${CLAUDE_KEY}
  #   baseUrl: https://api.anthropic.com/v1

gateways:
  lark:
    enabled: true
    appId: ${LARK_APP_ID}
    appSecret: ${LARK_APP_SECRET}
    verificationToken: ${LARK_TOKEN}

  # telegram:
  #   enabled: true
  #   botToken: ${TG_TOKEN}

  # discord:
  #   enabled: true
  #   botToken: ${DC_TOKEN}

# scheduler:
#   enabled: true
#   jobs:
#     - name: morning-report
#       cron: "0 7 * * *"
#       agent: agent0
#       message: "生成今日工作简报"

memory:
  dataDir: .omeclaw

runtime:
  port: 8080
